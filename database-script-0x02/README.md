This folder contains SQL inserts to populate the database with sample records.
How to run (PostgreSQL):
1. Ensure the schema is already created (run schema.sql)
2. psql -U <user> -d <db>
3. \i seed.sql
