# Database Schema (DDL)


This folder contains the SQL DDL to create the database schema for the project.


Files:
- schema.sql : creates tables, constraints and indexes.


How to run (PostgreSQL):
1. psql -U <user> -d <db>
2. \i schema.sql


Notes: Adjust SERIAL/AUTO_INCREMENT according to the RDBMS.
